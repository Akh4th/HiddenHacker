This script will automate hidden user creation ( as much as possible ).<br/>
Make sure python has Root / NT privileges before running the script.<br/>
Example : python3 GetUser.py --uname hacker --passwd P4$$w0rd
